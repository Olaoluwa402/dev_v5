const os = require("os");
// const app = require("./app");

//os: gives info about the current operating system
console.log(os);
//points to the current file directory
const dirPath = __dirname;
// console.log(dirPath);

//points to the current file path
const fleName = __filename;
// console.log(fleName);

//module - gives info about the current file
// console.log(module);

//process - gives info about the current environment
console.log(process);
